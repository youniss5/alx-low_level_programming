#include<stdio.h>
/**
 * main - Entry point
 * Description: 'print numbers'
 * Return: Always 0 (Success)
 */
int main(void)
{
int numb;
while (numb < 10)
{
printf("%d", numb);
numb += 1;
}
printf("\n");
return (0);
}
