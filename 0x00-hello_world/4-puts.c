#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print the statment to the user'
 * @parameter: we use (puts) to print out .
 *
 * Return: Always 0 (Success)
 */
int main(void)
{puts("\"Programming is like building a multilingual puzzle");
return (0);
}
